// ⚠️  AUTO-GENERATED by qa-ai-framework TestGenerator
// Review before merging – validate assertions match your API contract
// Generated: 2026-04-29T06:15:14.339Z

const { test, expect } = require('@playwright/test');
const ApiClient = require('../../src/utils/api-client');
const env = require('../../src/config/env');

const client = new ApiClient(env.apiBaseURL);

test.describe('[AI-Generated] POST /users', () => {
  test('POST /users – returns 2xx for valid request', async () => {
    // Type: happy_path

    const body = {
          "name": "Alice Smith",
          "job": "engineer"
    };
    const response = await await client.post('/users', body);

    // Assert: Status code is correct
    // Assert: Response body contains expected fields
    expect(response.status).toBe(201);
    
  });

  test('POST /users – missing required field \'name\'', async () => {
    // Type: negative

    const body = {
          "job": "engineer"
    };
    const response = await await client.post('/users', body);

    // Assert: Response indicates 'name' is required
    expect(response.status).toBe(400);
    
  });

  test('POST /users – missing required field \'job\'', async () => {
    // Type: negative

    const body = {
          "name": "Alice Smith"
    };
    const response = await await client.post('/users', body);

    // Assert: Response indicates 'job' is required
    expect(response.status).toBe(400);
    
  });

  test('POST /users – empty body returns 400', async () => {
    // Type: boundary

    const body = {};
    const response = await await client.post('/users', body);

    // Assert: Server rejects empty payload
    expect(response.status).toBe(400);
    
  });

  test('POST /users – unauthorized without token', async () => {
    // Type: security

    const body = {
          "name": "Alice Smith",
          "job": "engineer"
    };
    const response = await await client.post('/users', body);

    // Assert: Returns 401 Unauthorized
    expect(response.status).toBe(401);
    
  });
});
